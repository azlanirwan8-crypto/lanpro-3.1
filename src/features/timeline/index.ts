/**
 * Barrel timeline.
 *
 * Hanya re-export. Implementasinya ada di TimelinePanel.tsx — sebelumnya seluruhnya
 * ditulis di index.tsx, sehingga `import { TimelinePanel } from './features/timeline'`
 * menarik ribuan baris lewat berkas yang namanya tidak menyebut apa pun.
 */
export { TimelinePanel } from './TimelinePanel';
